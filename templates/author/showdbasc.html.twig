<table border="3">
    <tr>
        <td>Ref</td>
        <td>username</td>
        <td>email</td>
        <td>Action</td>
        <td>Delete</td>

    </tr>
    {%for a in author%}
    <tr>
        <td>{{a.id}}</td>
        <td>{{a.username}}</td>
        <td>{{a.email}}</td>
        <td><a href="{{path('app_editauthor',{'id':a.id})}}">edit</a></td>
        <td><a href="{{path('app_deleteauthor',{'id':a.id})}}">delete</a></td>

    </tr>
    {%endfor%}
</table>

       
